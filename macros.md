# Macros 
Macros is a prerocessor directive that defines a name or a function like macro that can be used throughtout the code.

## Note
- It is replace the name of macro to value of macro
- macro is defined using the '#define' preprocessor directive 

## Suntax
#define macro_name macro_value


















