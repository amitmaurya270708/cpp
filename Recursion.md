# Recursion
A function that calls itself called recursive function .

## Syntax
    return-type func-name(parameter){
        if(base-condition){
            // code
        }
        else{
            func-name(parameter);
        }
    }