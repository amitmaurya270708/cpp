# Encapsulatiomn
Encapsulation means binding data and methods within a class , providing control over the accessibility and it prevents external code from directly modifying the internal data of an object.

## Example
    class Person{
        private:
        // data
        public:
        function(){

        }
    };