# Function Overriding
Whenever we writing function in base and derived classes in such a way that function name, parameter must be a same called function overriding.

## Syntax
    class class-name{
        void fun(){

        }
    };
    class class-name:public base-class{
        void fun(){
            
        }
    }