# Inline Function
If a function is inline , then the compiler places the copy of the function code in place of the function call.
And this can speed up the program execution.

## Syntax 
    inline return_type func_name(parameter){
        // code
    }